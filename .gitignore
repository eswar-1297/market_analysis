# dependencies
node_modules/
client/node_modules/

# build output (run `npm run build` on deploy)
client/dist/

# secrets & local config — NEVER commit
.env
.env.local
*.key.json
service-account*.json

# on-disk caches (PageSpeed scores) — regenerated at runtime
data/.cache/
# MCP OAuth: dynamically-registered clients (runtime, per-deployment)
data/mcp-oauth-clients.json

# logs / runtime
*.log
.pm2/

# OS
.DS_Store
Thumbs.db
