{
  "name": "cloudfuze-marketing-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Marketing performance dashboard for CloudFuze lead-gen combinations (GA4, Search Console, Google Ads, Core Web Vitals) with a team-assessment layer.",
  "scripts": {
    "install:all": "npm install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "node --watch server/index.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "check": "node server/check.js",
    "auth": "node server/auth.js",
    "start": "node server/index.js"
  },
  "dependencies": {
    "@google-analytics/data": "^4.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "google-ads-api": "^17.1.0",
    "googleapis": "^140.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
